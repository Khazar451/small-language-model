# Small Language Model

This repository contains a small language model for various natural language processing tasks.

## Installation

To install the required packages, run:

```
pip install -r requirements.txt
```
